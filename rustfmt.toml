# Conservative, stable-only rustfmt settings so `cargo fmt --check` passes on
# stable toolchains in CI. No nightly-only options here.
edition = "2021"
max_width = 100
tab_spaces = 4
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
